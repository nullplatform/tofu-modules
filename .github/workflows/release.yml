name: pipeline tofu
on:
  push:
    branches:
      - main
jobs:
  release:
    uses: nullplatform/actions-nullplatform/.github/workflows/release.yml@main
    permissions:
      contents: write
      pull-requests: write